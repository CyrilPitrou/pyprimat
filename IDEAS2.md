In the output time evolution of all nuclides, one can replace the values of the nuclides in the eras where the were not solved (currently filled by 0) with their nuclear statistical equilibrium value (YA) so that plots are nice.

Make spectral_distortions = True the default value (in runs, in tests and in the gui).

The CLI should accept small_parthenope or anything which leads to a valid network file and not just small medium and large.