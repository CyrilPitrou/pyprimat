# run_small.ini -- the default ("small", 12-reaction) network at the
# standard SM baryon density. See CLAUDE.md's "Validation before
# committing" table (../CLAUDE.md, repo root) for the expected
# YPBBN/D-H reference values once the solver (Phase 5+) lands.
network = small
Omegabh2 = 0.022425
DeltaNeff = 0
numerical_precision = 1e-7
